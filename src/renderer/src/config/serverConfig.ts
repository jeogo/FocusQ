/**
 * إعدادات الخادم المركزي
 * هذا الملف يحتوي على إعدادات الاتصال بالخادم المركزي
 */

export const SERVER_CONFIG = {
  // عنوان IP الخادم المركزي - يجب تغييره عند نشر النظام
  SERVER_HOST: '192.168.1.14',
  
  // منفذ الخادم
  SERVER_PORT: 4000,
  
  // المدة التي يستغرقها الجهاز قبل اعتبار الاتصال مقطوعاً (بالمللي ثانية)
  CONNECTION_TIMEOUT: 15000,
  
  // عدد محاولات إعادة الاتصال
  RECONNECTION_ATTEMPTS: 15,
  
  // الفاصل الزمني بين محاولات إعادة الاتصال (بالمللي ثانية)
  RECONNECTION_DELAY: 1000,
  
  // إذا كانت هذه الشاشة هي شاشة العرض على الخادم المركزي
  IS_DISPLAY_SERVER: false
};

export default SERVER_CONFIG;
